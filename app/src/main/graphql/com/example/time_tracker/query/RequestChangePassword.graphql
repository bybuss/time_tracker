query RequestChangePassword(
    $id: UUID!,
    $firstName: String!,
    $lastName: String!,
    $email: String!,
) {
    requestChangePassword(
        findData: {
            id: $id
            firstName: $firstName
            lastName: $lastName
            email: $email
        }
    )
}