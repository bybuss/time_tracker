query GetFullTasksById($id: Int!) {
    getTask(
        searchData: { id: $id }
    ) {
        id
        name
        description
        isDone
        addedAt
        doneAt
        assignerId
        color
        duration
        difficulty
        projectId
        groupId
    }
}