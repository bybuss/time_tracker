mutation AddOrganization($name: String!, $description: String!) {
    addOrganization(
        data: {
            name: $name,
            description: $description
        }
    ) {
        id
    }
}